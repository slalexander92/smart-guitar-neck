import { convertToFlats, convertToSharps } from './convert-to-flats';
import {
  buildMajorScale,
  buildMinorScale,
  buildMajorPentatonicScale,
  buildMinorPentatonicScale,
  buildMixolydianScale,

  CHROMATIC_SCALE,
  MAJOR_SCALE_DEGREES,
  MINOR_SCALE_DEGREES,
  MAJOR_PENTATONIC_SCALE_DEGREES,
  MINOR_PENTATONIC_SCALE_DEGREES,
  MIXOLYDIAN_SCALE_DEGREES,
} from './build-scale';

export const utilities = {
  convertToFlats,
  convertToSharps,

  buildMajorScale,
  buildMinorScale,
  buildMajorPentatonicScale,
  buildMinorPentatonicScale,
  buildMixolydianScale,

  CHROMATIC_SCALE,
  MAJOR_SCALE_DEGREES,
  MINOR_SCALE_DEGREES,
  MAJOR_PENTATONIC_SCALE_DEGREES,
  MINOR_PENTATONIC_SCALE_DEGREES,
  MIXOLYDIAN_SCALE_DEGREES,
};
